import 'package:flutter/material.dart';

class Tidakadaintent extends StatelessWidget {
  const Tidakadaintent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Tidak ada intent");
  }
}
